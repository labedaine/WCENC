default:
    suites:
        tests:
            autoload:
                - %paths.base%/bootstrap
            paths: [%paths.base%/../tests/]
            contexts:
                - FeatureContext
                - PopulateSubContext
